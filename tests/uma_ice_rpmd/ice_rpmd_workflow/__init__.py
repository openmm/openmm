"""Shared UMA ice RPMD helpers (order parameters, thermo, LAMMPS/OpenMM geometry).

Import from this package for new code. Flat shims ``ice_order_parameters``,
``rpmd_thermo_utils``, and ``benchmark_same_structure_openmm_vs_lammps`` at the
parent directory still resolve for legacy scripts and tests.
"""
