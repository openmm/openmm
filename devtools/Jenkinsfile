pipeline {
    agent any

    stages {
        stage("Build and test") {
            agent {
                label "cuda && linux"
            }
            steps {
                sh 'git clean -fxd; git checkout .'
                sh 'CUDA_VERSION=9.0 bash -lex devtools/ci/jenkins/install.sh'
            }
        }
    }
}